#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
#include <cmath>
#include <limits.h>
#include <map>
#include <set>
#include <queue>
using namespace std;
using ll = long long;

int main() {
    int N, x, y; cin >> N >> x >> y;
    veci A(N+1), dpx(N+1), dpy(N+1);
    for(int i=1; i<N+1; i++) cin >> A[i];
}